# Project Profile

## Identity

- Project name: @ynode/ylog
- Purpose: Standalone and Fastify-compatible structured and human-readable logging.
- Intended users: Node.js application developers and maintainers of the @ynode ecosystem.
- Maturity: stable
- Visibility: public npm package and public GitHub repository.

## Applicable review standards

- Core: `.review/standards/CODING_STANDARDS.md`
- JavaScript: `.review/standards/JAVASCRIPT.md`
- TypeScript declarations: `.review/standards/TYPESCRIPT.md`
- Node.js: `.review/standards/NODEJS.md`
- Unix shell: `.review/standards/SHELL.md`
- Windows command scripts: `.review/standards/BATCH.md`
- GitHub Actions: `.review/standards/GITHUB_ACTIONS.md`
- Public repository: `.review/standards/PUBLIC_REPOSITORY.md`
- Public npm package: `.review/standards/PUBLIC_NPM_PACKAGE.md`
- Observability: `.review/standards/OBSERVABILITY.md`
- Root repository instructions: `AGENTS.md`
- Scoped instruction files: none currently.

## Supported environments

- Runtime contract: Node.js `>=20.19.0`; ESM package with public TypeScript declarations.
- Exact CI boundaries: Node.js 20.19.0, 22.13.0, and 24.0.0.
- Runtime policy: Node.js 20 is retained until the next package major despite upstream end of life; Node.js 22 and 24 are recommended. New majors require CI evidence.
- Package manager: npm 11.17.0 with the committed `package-lock.json`.
- Peer and service compatibility: No runtime peer dependencies.
- Platforms: Full gates run on Ubuntu; filename-safe hook logic also runs on Windows.
- CI trust model: GitHub-hosted runners, least-privilege job tokens, immutable third-party action SHAs, no publish token, and npm trusted publishing from the tag-only release workflow.

## Public contracts

- The logger factory and static methods, level hierarchy and environment defaults, structured fields, throttling, redaction, TTY and non-TTY formats, streams, and Fastify logger compatibility.
- `package.json#exports`, packed files, README examples, generated JSDoc, and `CHANGELOG.md` are release contracts.
- Only documented entry points are supported; internal `src` paths not exposed by `exports` are private.

## Architecture and ownership

- Trust and external boundaries: Caller messages and objects, Error values, environment variables, stdout/stderr capabilities, terminal rendering, process identity, and repeated-error state.
- Runtime code owns behavior; `index.d.ts` owns the matching public type surface.
- Repository scripts own deterministic docs, packed-consumer checks, staged-file checks, and release preparation.
- Generated documentation lives in ignored `docs/` and must be regenerated from source.

## Consequential project invariants

### Invariant 1: Defaults resolve at logger creation

- Requirement: Defaults resolve at logger creation.
- Risk if violated: Reading environment-derived levels at module import makes test and application configuration order-dependent.
- Safe path: Resolve mutable environment policy lazily and validate global changes before replacing the current default.
- Authoritative evidence: public documentation, declarations, source, and focused tests in this repository.

### Invariant 2: Logs do not become a disclosure or terminal-control channel

- Requirement: Logs do not become a disclosure or terminal-control channel.
- Risk if violated: Secrets, control characters, circular values, or thrown accessors can leak data or break the owning application.
- Safe path: Redact recursively with bounds, sanitize text output, preserve safe causes, and keep logging failure-tolerant.
- Authoritative evidence: public documentation, declarations, source, and focused tests in this repository.

## Security and privacy

- Vulnerability reporting and supported versions are defined in root `SECURITY.md`.
- Secrets, credentials, private paths, and reusable tokens must not enter source, fixtures, logs, workflow output, packed artifacts, or generated documentation.
- Dependency updates are reviewed weekly. Production advisories fail at moderate severity and the complete development tree fails at high severity.
- Accepted security exceptions require an owner, reachability rationale, remediation target, and expiry; none are currently recorded.

## Documentation and validation

- User documentation: `README.md`
- Contributor and release documentation: `CONTRIBUTING.md`
- API documentation: source JSDoc generated by `npm run docs`
- Release history: `CHANGELOG.md`, generated before the release commit and signed tag.

| Purpose | Command | Preconditions | Safe for review-only use |
| --- | --- | --- | --- |
| Full publication gate | `npm run prepublishOnly` | installed lockfile dependencies; some tests may bind loopback or use optional services | yes |
| Format | `npm run format:check` | installed dependencies | yes |
| Lint | `npm run lint` | installed dependencies | yes |
| Tests | `npm test` | installed dependencies; optional service tests may skip | yes |
| Type declarations | `npm run --if-present test:types && npm run --if-present typecheck` | installed dependencies; offline | yes |
| Package inspection | `npm pack --dry-run --ignore-scripts --json` | none beyond npm | yes |
| Security | `npm audit --omit=dev --audit-level=moderate && npm audit --audit-level=high` | registry access | yes |
| Release identity | `npm run release:check -- vX.Y.Z` | clean SemVer manifest and matching changelog | yes |

## Release and versioning

- SemVer governs public behavior and types.
- Development commits may carry autover build metadata; such versions are never publishable.
- Run `npm run release:patch`, `npm run release:minor`, or `npm run release:major` on clean synchronized `main`.
- The helper validates the bumped package, creates a signed and signed-off commit plus signed tag, and never pushes.
- Push with `git push --follow-tags`; `.github/workflows/release.yml` verifies the signed tag and publishes the exact tested tarball through npm OIDC with provenance.
- The npm trusted publisher must be restricted to this repository and `.github/workflows/release.yml`.

## Known constraints and accepted debt

- Node.js 20 is upstream-EOL but remains in the current major compatibility contract. Remove it at the next major release and update `engines`, CI, README, and `SECURITY.md` together.
- Local service integration is conditional where no disposable service is configured; skipped tests are not evidence of live integration behavior.
- No additional accepted debt is recorded.
